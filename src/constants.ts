/**
 * Default time (milliseconds) to wait before next line starts render
 */
export const DEFAULT_DELAY_BETWEEN_LINES = 0
/**
 * Default time (milliseconds) to wait before next character starts render
 */
export const DEFAULT_DELAY_PER_CHARACTER = 50
